# MapWarm — 걸어서 땅따먹기 🗺️

> splix.io를 현실 세계로 가져온 모바일 게임.
> 실제로 걸으면 지도 위에 선이 그어지고, 출발한 내 영토로 돌아오면 둘러싼 땅이 내 것이 된다.

## 0. 목표와 제약 (사용자 확정)

- **최종 목표**: 실제 현실의 땅을 기반으로 **친구들과 함께** 하는 모바일 게임
- **인원 유연성**: 적은 수(친구 2~3명)든 많은 수든 즐길 수 있어야 함 — 부족한 인원은 봇으로 채운다
- **그래픽**: 고사양 금지. 버벅이지 않는 최소한의 즐거운 그래픽 (성능 예산: 중저가 폰에서 60fps)
- **예산**: 총 **$100** — 서버·도메인 포함 전체 비용이 이 안이어야 한다
  - 전략: v0.1은 서버 없이 $0. v0.2 멀티는 무료 티어(예: Cloudflare Workers, Supabase Realtime) 기반 —
    친구 규모 동시접속은 무료~월 $5 수준이라 예산 내 충분
- **제출물**: 폰 브라우저로 여는 웹앱 (홈화면 추가로 앱처럼 사용, 앱스토어 비용·심사 없음 → 예산 절약)

## 1. 핵심 컨셉

- **장르**: 위치기반(GPS) 영토 점령 게임
- **플랫폼**: 모바일 웹 (설치 없이 브라우저로 실행, 폰 홈화면 추가 가능)
- **한 판의 흐름**:
  1. 시작하면 내 위치 주변에 작은 시작 영토(3×3 칸)가 생긴다
  2. 영토 밖으로 걸어나가면 **꼬리(trail)** 가 그려진다
  3. 꼬리를 끌고 다시 내 영토로 돌아오면 → 꼬리로 둘러싼 안쪽 땅이 전부 내 영토가 된다
  4. 꼬리가 그려진 상태에서 **자기 꼬리를 밟으면 사망** (splix.io 규칙 그대로)
  5. 봇(AI 상대)도 같은 규칙으로 땅을 먹는다. 봇의 꼬리를 끊으면 봇을 제거

## 2. 세계 ↔ 게임 좌표 변환

현실 세계를 격자(그리드)로 나눈다:

- **1칸 = 현실의 약 5m × 5m** (위도/경도를 격자 좌표로 변환)
- GPS 좌표(위도, 경도) → 격자 셀 (col, row) 로 스냅
- 게임판 크기: 시작 위치 중심으로 **80×80칸** (현실의 약 400m × 400m 동네 한 바퀴 스케일)

## 3. 조작 모드 (중요)

| 모드 | 용도 | 조작 |
|---|---|---|
| **데모 모드** (기본) | 데스크톱/실내 테스트 | 화면 가상 조이스틱 또는 방향키로 이동 |
| **GPS 모드** | 실제 플레이 | 폰 위치 권한 허용 후 실제로 걸어서 이동 |

- 앱은 데모 모드로 시작한다. GPS 모드는 상단 토글로 전환 (위치 권한 요청)
- GPS 모드에서는 위치 정확도가 낮으면 (>25m) 경고 배지 표시

## 4. 화면 구성 (모바일 세로 기준)

```
┌─────────────────────────┐
│ 상단바: 점유율 % · 모드 토글 · 테마 │
│                         │
│                         │
│      게임 캔버스          │  ← 내 위치 중심으로 카메라 따라감
│   (격자 + 영토 + 꼬리)     │
│                         │
│                         │
│   ◯ 가상 조이스틱 (데모)   │  ← 왼손/오른손 어느 쪽이든 터치한 곳에 생김
└─────────────────────────┘
```

- **내 영토**: 파랑 계열 / **내 꼬리**: 반투명 파랑 점선 느낌
- **봇 영토/꼬리**: 봇마다 다른 색 (빨강, 노랑)
- 사망 시: 짧은 연출 후 "다시 시작" 오버레이
- 점유율(내 영토 칸 수 ÷ 전체 칸)이 상단에 항상 보임

## 5. 봇 (v0.1 기준 2마리)

- 플레이어와 같은 규칙: 나가서 꼬리 그리고, 돌아오면 점령
- 행동: 자기 영토 밖으로 사각형에 가까운 루프를 돌고 복귀 (단순 패턴이면 충분)
- 봇도 자기 꼬리 밟으면 죽고, 잠시 후 다른 위치에서 리스폰

## 6. 기술 스택 (빌드 도구 없이 단순하게)

- **순수 HTML + CSS + JavaScript** — `index.html`, `css/`, `js/` 구조
- 렌더링: `<canvas>` 하나. **실제 지도 배경**: OpenStreetMap 타일 이미지(무료·API 키 불필요)를
  캔버스 맨 밑에 깔고 그 위에 격자·영토·꼬리를 반투명으로 그린다.
  - 출처 표기 "© OpenStreetMap contributors" 화면 구석에 필수
  - 데모 모드 기준 위치: 서울시청 (37.5665, 126.9780) — 지도가 항상 보이도록
  - 타일 로드 실패/오프라인이면 지도 없이 격자만 (게임은 계속)
  - 라이브러리 없이 타일 URL을 직접 계산해 `drawImage`로 그린다 (타일은 이미지 데이터라 "외부 라이브러리 금지" 예외)
- GPS: 브라우저 `navigator.geolocation.watchPosition`
- 저장: `localStorage` (최고 점유율 기록, 테마 설정)
- 테마: 라이트/다크/시스템 3단 토글 (UX 아키텍트 표준)

## 6.5 아트 디렉션 (✅ 컨펌 완료 — Paper 시안 B/C 기준)

> **확정**: 시안 B(캔디 스티커 팝) = **라이트 모드**, 시안 C(네온 나이트 스티커) = **다크 모드**.
> 시안 파일: https://app.paper.design/file/01M1EC9EG0NAGK4BW21Y5J85SN
>
> **확정 팔레트** —
> 라이트: 지면 `#EAEBE7` · 칩 차콜 `#2B2B2E` + 흰 스티커 테두리 · 플레이어 라임 `#B7E819` · 봇1 핑크 `#FF3E9A` · 봇2 오렌지 `#FF8A00`
> 다크: 지면 `#121214` · 칩 `#1C1D21` · 플레이어 라임 `#C8F31D`(글로우) · 핑크/오렌지 동일 · 본문 `#F2F2F4` · 보조 `#7C7D84`
> 지도는 두 모드 모두 채도를 강하게 뺀다 (캔디 영토가 주인공).

무드: **"하드웨어 토이 × 할프톤 스티커"** — 기계 장난감 패널(리벳·플레이트) UI 위에
영토가 망점(할프톤) 도트로 찍히는 팝한 그래픽.

### 팔레트 (전부 토큰으로)
- 지면(라이트): 웜 오프화이트 / 지면(다크): 딥 차콜
- UI 크롬: 라이트그레이 플레이트 + 차콜 패널 (버튼·칩은 패널 위 요소)
- **히어로 액센트: 네온 라임(chartreuse)** — 플레이어 색이자 UI 포인트
- 캔디 서브: 핫핑크(봇1), 오렌지(봇2). 파랑은 쓰지 않는다 (Tailwind 기본색 탈피)
- 지도 타일: 채도를 강하게 뺀 밝은 워시 — 지도는 무대, 캔디 영토가 주인공

### 영토·엔티티 렌더링 (캔버스)
- **영토 = 할프톤 도트**: 셀마다 원을 찍되, 8방 이웃 중 내 영토 수에 비례해 점 크기 변화
  (내부 = 셀의 ~72% 굵은 점, 가장자리 = 잘게). 면 채우기 금지
- 꼬리: 둥근 캡슐 라인 (도트 아님 — 영토와 구분)
- 머리: 라운드 사각 스티커 + 흰 테두리 + 얕은 그림자
- **점령 순간 연출 필수**: 새로 먹은 도트들이 팝(스케일 인)하고 점유율 칩이 한 번 튄다

### 스티커 시스템 (추가 레퍼런스 반영)
- 모든 UI 칩·패널은 **스티커**다: 두꺼운 테두리(2.5~3px, 캔디색 또는 흰색) + 은은한 글로우/그림자
  — 오려 붙인 느낌. 납작한 사무용 카드 금지
- 숫자(점유율·기록)는 **전광판 픽셀 느낌**: 시스템 모노 볼드 + 대문자 + 자간, 하이라이트 필 박스 안에
- 팔레트 확장: 핫핑크·네온 라임·오렌지·스카이블루 캔디 4색 + 차콜/오프화이트 지면

### 플레이어 아바타 마커 (신규 — 부루마블 말)
- **모든 유저(플레이어·봇·향후 친구)는 프로필 아바타가 자기 머리 위치를 따라다닌다**
  — 보드게임 말처럼. 카메라가 아니라 각자의 현재 셀 위에 떠 있음
- 아바타 = 원형 사진/그림 + **블로비 스티커 테두리**(자기 색, 두껍고 물결지게) + 아래로 꼬리(말풍선 꼬리)
- 이동 시 살짝 통통 튀는(bob) 애니메이션, 죽으면 아바타가 훽 넘어지는 연출
- v0.1: 기본 아바타 = 캐릭터 이모지 얼굴 (플레이어 🙂, 봇은 각자 다른 얼굴). 사진 업로드는 멀티(v0.2)에서

### 타이포·컴포넌트
- 라벨: 시스템 모노(ui-monospace 계열) **대문자 + 자간 넓게** = 스텐실 각인 느낌 (폰트 CDN 금지 유지)
- HUD: 불투명 흰 띠 금지 — 지도 풀블리드 위에 **떠 있는 스티커 칩**
- 사망 오버레이: 차콜 하드웨어 패널 카드 + 라임 "다시 시작" 버튼 (스티커 테두리)
- 테마 토글: 이모지 금지 → 인라인 SVG 아이콘
- 다크 모드: 차콜 지면 + 네온 유지 (네온은 다크에서 더 산다)

## 7. v0.1 완성 조건 (Definition of Done)

- [ ] 데모 모드에서 조이스틱으로 움직여 땅을 먹을 수 있다
- [ ] 꼬리로 둘러싼 영역이 정확히 채워진다 (flood fill)
- [ ] 자기 꼬리를 밟으면 죽고 다시 시작할 수 있다
- [ ] 봇 2마리가 돌아다니며 땅을 먹는다
- [ ] 봇 꼬리를 끊으면 봇이 죽는다
- [ ] GPS 모드 토글이 있고, 위치 권한을 받아 실제 좌표로 이동한다
- [ ] 모바일 세로 화면에서 레이아웃이 깨지지 않는다 (375×812 기준)
- [ ] 점유율 % 와 최고 기록이 표시된다

## 8. v0.2 — 친구 멀티플레이 (다음 단계, 지금은 안 함)

핵심 목표가 멀티이므로 v0.1 코드는 멀티를 가정하고 짠다:
게임 상태(격자·꼬리·점령)는 "누가 조작하든" 동일하게 동작하는 순수 로직으로 유지 —
봇이 쓰는 코드와 원격 친구가 쓰는 코드가 같아야 한다.

- **방 방식**: 방 코드(6자리)를 친구에게 공유 → 같은 게임판 입장. 빈 자리는 봇이 채움
- **서버 후보 (예산 $100 내)**: Cloudflare Workers + Durable Objects (무료 티어) 또는
  Supabase Realtime (무료 티어). 친구 규모(~10명 동시)는 무료 범위
- **동기화**: 셀 단위 이동이라 보낼 데이터가 아주 작음 (초당 몇 바이트) — 저사양·저예산에 유리
- 실제 지도 타일(OpenStreetMap) 오버레이, 하루/주간 리더보드는 그 다음
