# MapWarm 프로젝트 규칙

splix.io를 현실 세계로 가져온 GPS 땅따먹기 모바일 웹 게임.
기획의 단일 기준 문서는 `GAME_DESIGN.md` — 스펙 충돌 시 이 문서가 이긴다.

## 사용자에 대해

- 사용자는 **디자이너**다. 개발 용어를 쓸 때는 한 줄로 풀어서 설명할 것.
- 보고는 한국어로.

## 기술 규칙

- 빌드 도구 없음. 순수 HTML/CSS/JS. `index.html` + `css/` + `js/` 구조 유지.
- 외부 라이브러리/CDN 금지 (v0.1 기준). 캔버스 직접 렌더링.
  - 예외: OpenStreetMap 지도 타일 **이미지**는 허용 (코드가 아니라 데이터. 출처 표기 필수).
- 모바일 우선: 375×812 세로 화면이 1순위, 데스크톱은 테스트용.
- 테마: 라이트/다크/시스템 3단 토글 필수 (`.claude/agents/ux-architect.md` 표준 준수).
- CSS는 디자인 토큰(CSS 변수) 기반 — 하드코딩 색상 금지.

## 에이전트 팀

| 에이전트 | 역할 |
|---|---|
| `ux-architect` 📐 | CSS 시스템·레이아웃·UX 구조 설계 |
| `game-builder` 🔨 | 게임 로직·렌더링 구현 |
| `playtester` 🎮 | 브라우저로 실제 플레이하며 버그·UX 문제 리포트 |

개발 루프는 `/game-loop` 스킬로 돌린다: 빌드 → 플레이테스트 → 수정.

## 검증

- 변경 후에는 반드시 브라우저(모바일 뷰포트 375×812)로 열어 직접 조작해 확인.
- 콘솔 에러 0개가 기본. 에러가 있으면 완료가 아니다.
